# Merge into the target project's pyproject.toml; do not replace it wholesale.
[tool.ruff.lint]
extend-select = ["C90"]

[tool.ruff.lint.mccabe]
max-complexity = 10

[tool.mypy]
python_version = "3.12"
check_untyped_defs = true
disallow_untyped_defs = true
