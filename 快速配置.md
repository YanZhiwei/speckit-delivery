# SD 快速配置

本指南将一个已有项目接入 Spec Kit Delivery（SD）。示例覆盖多模块、前后端混合项目，
但不绑定某个仓库、目录名或包管理器。

目标不是复制一份“规范文档”，而是让每条规则都有三个位置：**编辑前提示、编辑后
验证、任务关闭阻断**。项目的 `.specify/` 可以被 Git 忽略；ADR、原生工具配置和
CI 仍应由项目跟踪。

## 0. 先确定项目事实

先写下下面六项，再运行安装命令。不要让 SD 猜测。

| 项目事实     | 示例                                                | 填写位置                    |
| ------------ | --------------------------------------------------- | --------------------------- |
| 代码族       | `frontend/**/*.ts(x)`、`backend/**/*.py`            | Quality Profile `match`     |
| 原生命令     | `pnpm lint:ci`、`pnpm typecheck`、`uv … ruff check` | Quality Profile `commands`  |
| 架构规则     | Router 不承载业务规则；依赖向内                     | 项目架构文档 / ADR          |
| 复杂度规则   | Python C901 ≤ 10                                    | Ruff 配置，不能只写在文档   |
| 注释规则     | 解释非显然意图，不记录实现流水账                    | 风格文档 + Standards Review |
| 耐久决策目录 | `docs/architecture/adr/`                            | Delivery / Decision 配置    |

若某项尚未有可执行机制，标为缺口；不要配置成“通过”。例如 Ruff 的
`max-complexity = 10` 必须同时启用 `C90`，否则复杂度门禁没有生效。

## 0.1 推荐的项目治理目录

SD 不要求固定目录，但建议将**耐久、供人和 Agent 共同阅读的规则**集中在
`docs/engineering/` 与 `docs/architecture/`；将**可执行规则**保留在语言原生配置中。

```text
AGENT_GUIDE.md                         # 入口、模块概览、日常质量命令
docs/
├── engineering/
│   ├── quality.md                      # 质量命令、CI / Hook 责任和升级策略
│   ├── code-style.md                   # 跨语言规则：DRY、注释意图、函数职责
│   ├── code-style-typescript.md        # TypeScript / React 细则
│   └── code-style-python.md            # Python / FastAPI 细则
└── architecture/
    ├── boundaries.md                   # 模块归属、分层与依赖方向
    ├── testing-policy.md               # 测试目录、命名与例外
    └── adr/                            # 已接受和 Proposed 的架构决策

eslint.config.* / tsconfig*.json        # TypeScript 可执行规则
pyproject.toml / ruff.toml              # Python 可执行规则
.husky/ 或 .pre-commit-config.yaml      # 本地快速反馈
.github/workflows/                      # CI 的可复现门禁
```

建议的归属如下：

| 规则                           | 放在哪里                                    | 为什么                            |
| ------------------------------ | ------------------------------------------- | --------------------------------- |
| 格式、lint、类型、测试、复杂度 | 语言原生配置与 CI                           | 机器必须能运行和失败              |
| 质量命令、Hook/CI 分工         | `docs/engineering/quality.md`               | 让人和 SD 知道命令的证据等级      |
| DRY、职责、注释意图            | `docs/engineering/code-style.md`            | 属于跨语言语义规则                |
| 框架细则                       | `docs/engineering/code-style-<language>.md` | 避免跨语言规则混在一个文件        |
| 模块归属、分层、依赖方向       | `docs/architecture/boundaries.md`           | 属于系统结构而非代码格式          |
| 测试目录、命名、例外与迁移     | `docs/architecture/testing-policy.md`       | 先声明目录契约，再用 checker 阻断 |
| 方案取舍和历史背景             | `docs/architecture/adr/`                    | 供后续 Spec / ADR 检索与引用      |

不要将 DRY、边界或注释规则只写在 `AGENT_GUIDE.md`：入口文件应保持短小并链接上述
权威文档。也不要把机械规则只写进 Markdown；必须同时落到原生工具。

## 1. 安装扩展与 workflow

以下示例在目标项目根目录执行，`$delivery` 改为本仓库的本地路径；发布后可换成
catalog 或 zip 安装。

```powershell
$delivery = 'D:\Repos\Github\speckit-delivery'

specify extension add --dev "$delivery\extensions\decision"
specify extension add --dev "$delivery\extensions\ralph"
specify extension add --dev "$delivery\extensions\review"
specify extension add --dev "$delivery\extensions\simplify"
specify extension add --dev "$delivery\extensions\evidence"
specify extension add --dev "$delivery\extensions\quality"
specify extension add --dev "$delivery\extensions\docs-sync"
specify extension add --dev "$delivery\extensions\delivery"

specify workflow add --dev "$delivery\workflows\feature-delivery\workflow.yml"
specify workflow add --dev "$delivery\workflows\bugfix-delivery\workflow.yml"
specify workflow add --dev "$delivery\workflows\lightweight-delivery\workflow.yml"
```

已有项目升级时，对变更的 extension 使用 `--force`。随后运行 `specify extension list`
和 `specify workflow list`；至少应看到 `quality`（2 commands）、`delivery`（5 commands）
和 `review`（2 commands）。

## 2. 初始化项目治理

执行 `speckit.delivery.init`，然后人工核对：

- ADR 路径是否指向项目真正跟踪的目录；
- `docs-sync` 是否 `enabled: true`，且只同步有权威来源的文档；
- `.specify/` 是本地临时产物还是要提交；两种模式均可，但 ADR 不能引用被忽略的 Spec；
- 根治理文档是否声明分支、模块边界、代码风格和质量命令。

可采用 `.specify/` 忽略、`docs/architecture/adr/` 跟踪的模式：Spec/Plan/Tasks 是个人
工作状态，Accepted ADR 是供下一次 SD 检索的耐久决策。

## 3. 配置质量 Profile

创建 `.specify/extensions/quality/quality-config.yml`。以下是可直接改造的多模块示例：

```yaml
schema_version: "1.0"

policy_sources:
  architecture:
    - AGENT_GUIDE.md
    - docs/architecture/boundaries.md
    - docs/architecture/testing-policy.md
    - docs/architecture/adr
  style:
    - docs/engineering/code-style.md
    - docs/engineering/code-style-typescript.md
    - docs/engineering/code-style-python.md

standards:
  - id: typescript-native-checks
    owner: quality
    mechanism: "pnpm lint:ci, pnpm typecheck, pnpm test:frontend"
  - id: dry-and-module-ownership
    owner: review
    mechanism: "speckit.review.standards against architecture policy"
  - id: test-layout
    owner: quality
    mechanism: "pnpm check:test-layout"
  - id: simplification
    owner: simplify
    mechanism: "speckit.simplify.scan before final review"
  - id: architecture-decisions
    owner: adr
    mechanism: "speckit.decision.propose and speckit.decision.finalize"

profiles:
  - id: frontend-typescript
    match: ["frontend/**/*.{ts,tsx,js,jsx}", "gateway/**/*.{ts,tsx}"]
    discovery_files: [package.json, pnpm-workspace.yaml]
    commands:
      format: "pnpm format:check"
      lint: "pnpm lint:ci"
      typecheck: "pnpm typecheck"
      test: "pnpm check:test-layout && pnpm test:frontend"
    rules:
      complexity_max: 10
      comment_policy: "Explain non-obvious intent; do not use history comments as a design record."

  - id: project-automation
    match: ["scripts/**/*.{ts,js,mjs,cjs}", ".github/workflows/**/*.{yml,yaml}"]
    discovery_files: [package.json]
    commands:
      format: "pnpm format:check"
      test: "pnpm test:scripts"

unprofiled_changed_paths: block
# 一次性原型不纳入产品 SD 闸门；会报告排除路径，但不视为已验证。
excluded_paths: ["poc/**"]
reuse_passing_hooks: true
```

这段配置是规则与 SD 的连接点：`policy_sources` 会被 Quality Brief 和 Standards Review
按改动范围读取；`profiles.match` 把文件映射到真实命令；`standards` 把 DRY、边界、
注释和 ADR 这类语义规则映射给 Review、Simplify 或 ADR 生命周期。Delivery 初始化后，
还应在以下两个文件保持 ADR 与入口文档一致：

```yaml
# .specify/extensions/decision/decision-config.yml
adr_directories: [docs/architecture/adr]
context_files: [AGENT_GUIDE.md, docs/architecture/boundaries.md]

# .specify/extensions/delivery/delivery-config.yml
context_files: [AGENT_GUIDE.md, docs/engineering/quality.md]
adr_directories: [docs/architecture/adr]
docs_sync: auto
```

一个 Profile 只代表一类**实际能执行**的源码。Python、Go、Java 等应各自另建 Profile。
还没有可信命令的源码族不要加入 Profile；`unprofiled_changed_paths: block` 会阻止它被
错误地关闭。

## 4. 将规则变成可执行检查

SD 不实现语言工具；它只按变更范围调用项目已有工具并保存结果。请在项目自身配置中
启用规则，再在 Profile 引用相应命令。

| 规则                   | 原生机制                                       | SD 的职责                                 |
| ---------------------- | ---------------------------------------------- | ----------------------------------------- |
| 格式、lint、类型、测试 | Prettier / ESLint / TypeScript / Pytest 等     | 编辑后执行，失败阻断                      |
| Python 复杂度 ≤ 10     | Ruff `select` 含 `C90` + `max-complexity = 10` | 调 Ruff，报告真实结果                     |
| 注释意图               | 项目规范；可选自定义 checker                   | Quality Brief 提示，Standards Review 判断 |
| DRY、职责、目录边界    | 架构规范、依赖边界 checker（若有）             | Simplify / Standards Review 阻断          |
| 测试目录布局           | 项目自有 `check:test-layout`（仅检查明确路径） | 编辑前提示；失败阻断 Task                 |
| 架构方案               | ADR                                            | Proposed → reviewed HEAD → Accepted       |

不要尝试用一个“注释检查器”判定 DRY 或 OOD；这些需要结合 diff、调用方和架构约束的
语义审查。反过来，也不要仅靠 Review 检测类型错误或复杂度。

## 5. 配置 Hook：快速反馈，不替代关闭闸门

Hook 的作用是尽早反馈；Ralph 的 Quality + Standards 双判定才是任务关闭条件。

### Node / Husky 示例

```json
{
  "scripts": {
    "precommit:quality": "lint-staged",
    "prepush:quality": "pnpm lint:ci && pnpm typecheck && pnpm test"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx}": ["eslint --fix", "prettier --write"]
  }
}
```

`pre-commit` 应只运行快速、改动范围小的检查；`pre-push` 可以运行全仓类型检查和测试。
命令必须与 `quality-config.yml` 的对应命令一致或更严格。若 Hook 被绕过，Quality Check
仍会重新执行或核对当前 HEAD 的回执。

### Python / pre-commit 示例

```yaml
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.12.0 # 按项目锁定版本
    hooks:
      - id: ruff-check
        args: [--select, "E,F,I,C90"]
      - id: ruff-format
```

同时在项目 `pyproject.toml` 配置：

```toml
[tool.ruff.lint]
select = ["E", "F", "I", "C90"]

[tool.ruff.lint.mccabe]
max-complexity = 10
```

实际团队可采用不同 Hook 管理器；关键是不让 Hook 成为唯一门禁，也不让 Hook 执行与 CI
或 Quality Profile 相互矛盾的命令。

## 6. 首次验收与日常使用

首次配置后按顺序做一次真实验证：

```text
1. specify extension list / specify workflow list
2. speckit.delivery.doctor
3. 对一个小型真实改动执行 speckit.quality.brief
4. 执行 Profile 中每条命令
5. speckit.quality.check + speckit.review.standards
6. 确认 Ralph 仅在两种 verdict 都 pass 后关闭 Task
```

日常从 `$sd <需求>` 或 `/sd <需求>` 开始即可。Feature 会自动经过 Doctor、Spec、ADR、
Tasks 和 Ralph；Bugfix 与 Lightweight 走比例更合适的路线。直接运行 Ralph 前，必须已有
通过 `speckit.analyze` 的 `tasks.md`。

## 7. 新项目接入检查表

- [ ] 每种源码语言有对应的 Profile，或明确被 `block`。
- [ ] 每个 Profile 的命令能在项目根目录直接执行。
- [ ] 复杂度规则在原生工具中真的启用。
- [ ] DRY、边界、注释、ADR 都在 `standards` 声明 owner 和 mechanism。
- [ ] Hook 只负责快速反馈，CI/Quality Check 才有最终可复现证据。
- [ ] Docs Sync 已启用，且权威源和生成投影定义清楚。
- [ ] `.specify/` 的忽略/跟踪策略已决定；ADR 不依赖被忽略的文件。
- [ ] `speckit.delivery.doctor` 结果为 `pass` 后再开始实际 Feature。

相关说明：[English](QUICK-CONFIGURATION.md)、[质量闸门](QUALITY-GATE.zh-CN.md)、[架构](ARCHITECTURE.zh-CN.md)、
[生命周期](LIFECYCLE.zh-CN.md)、[分发](DISTRIBUTION.zh-CN.md)。
