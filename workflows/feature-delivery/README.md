# Feature Delivery Workflow

The full lane for new behavior and architectural change. Version `0.1.x` uses explicit gates and three safe no-op convergence iterations because Spec Kit command-step prose is not a machine-readable semantic result. Later versions will use a state protocol and custom result gate.
