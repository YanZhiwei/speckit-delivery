# Spec Kit Delivery Core

Creates the project-local delivery configuration and renders a final pull-request handoff. The configuration is project-specific; the distributed defaults do not assume a language, framework, ADR path, or verification command.
